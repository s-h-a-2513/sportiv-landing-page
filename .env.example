# Sportiv landing — environment template
# Copy to landing/.env for local Vite, or set in Vercel → Environment Variables.
# Vite exposes only VITE_* to the client (public Formspree form IDs, not secrets).

VITE_FORMSPREE_WAITLIST_ID=
VITE_FORMSPREE_CONTACT_ID=
